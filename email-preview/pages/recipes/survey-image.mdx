import SurveyImage, {
    Features,
    template,
} from "../../components/recipes/SurveyImage";
import LinkButton from "../../components/LinkButton";
import Markup from "../../components/Markup";
import PreviewLayout from "../../components/email/PreviewLayout";
import ToggleSwitch from "../../components/ToggleSwitch";
import {
    PreviewComponent,
    PreviewMarkup,
} from "../../components/PreviewContext";
export default ({ inline, children }) => {
    return (
        <PreviewLayout inline={inline} tools>
            {children}
        </PreviewLayout>
    );
};

{/* console.log('survey import', SurveyImage) */}

# Survey Recipe: Image

This recipe uses a hero module and survey module to track a user’s satisfaction with their HP experience.

<LinkButton
    href="https://litmus.com/checklist/public/z9A30hwvqG0Y7gFD"
    target="_blank"
    label="View in Litmus"
/>

## Features

<Features />

## Preview

<PreviewComponent label="surveyimage" Component={SurveyImage} />

## HTML

<PreviewMarkup label="surveyimage" template={template} />
