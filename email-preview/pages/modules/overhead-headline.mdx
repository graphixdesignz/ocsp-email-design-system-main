import OverheadHeadline, {
    Features,
    template,
} from "../../components/email/OverheadHeadline";
import LinkButton from "../../components/LinkButton";
import Markup from "../../components/Markup";
import PreviewLayout from "../../components/email/PreviewLayout";
import ToggleSwitch from "../../components/ToggleSwitch";
import {
    PreviewComponent,
    PreviewMarkup,
} from "../../components/PreviewContext";
export default ({ inline, children }) => {
    return (
        <PreviewLayout inline={inline} tools>
            {children}
        </PreviewLayout>
    );
};

# Overhead Headline

Similar to the Hero Headline, the OverheadHeadline partners a large image with a headline, subhead, body copy, and a CTA.

The Headline precedes the image, making it useful when important copy must appear first in the email.
Use to open an email or an important section within an email.

<LinkButton
    href="https://litmus.com/checklist/emails/public/d13781b"
    target="_blank"
    label="View in Litmus"
/>

## Features

<Features />

## Preview

<PreviewComponent label="overheadheadline" Component={OverheadHeadline} />

## HTML

<PreviewMarkup label="overheadheadline" template={template} />
